{{> header }}
<div class="container">
  <h1>{{ title }}</h1>
  <div class="row">
    <div class="col-sm-12">
      {{#if owner}}
        <a href="#/rooms/new">Nieuw</a>
      {{/if}}
        <button>Filter</button>
        <button>Mode</button>
    </div>
  </div>
  <div class="row">
    {{#if rooms}}
    <p>look at that... rooms!</p>
    {{else}}
    <p>This bitch empty</p>
    {{/if}}
  </div>
</div>
{{!-- <div class="page list">
  <h1>{{title}}</h1>
  <div class="body">
    {{#if loading}}
      Loading data, please wait...
    {{else}}
    <ul>
      {{#each posts}}
        <li>{{this.title}}</li>
      {{/each}}
    </ul>
    {{/if}}
    Go to <a href="/" data-navigo>Home page</a>
  </div>
</div> --}}
{{> footer }}